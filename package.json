{
  "name": "vaultkey",
  "private": true,
  "workspaces": [
    "password-manager-front",
    "password-manager-back"
  ],
  "scripts": {
    "dev:front": "npm run dev --workspace password-manager-front",
    "dev:back": "npm run dev --workspace password-manager-back",
    "build": "npm run build --workspace password-manager-front",
    "lint": "npm run lint --workspace password-manager-front",
    "test": "npm test --workspace password-manager-back"
  }
}
